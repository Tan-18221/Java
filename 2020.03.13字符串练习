package ta;
/*
字符串比较方法
public boolean equals(Object obj),参数可以是任何对象
任何对象都可以用object接受
可以直接使用
 */
public class tu {
    public static void main(String[] args) {
        String a="abub";
        String b="jnjn";
        char[]chararray={'h','e'};
        System.out.println(a.equals(b));
        }
    }

package ta;
/*
字符串获取方法
public int length(), .length()获取字符串当中含有的字符个数，拿到字符串长度
public string concat(string str) .concat(参数)将当前字符串和参数字符串拼接成为返回值新的类型
public char charAt(int index)获取指定索引位置的单个字符
public int indexOf(String str)查找参数字符串在本字符中首次出现的位置，如果没有返回值-1
 */
public class tu {
    public static void main(String[] args) {
        //获取字符串长度
       int a="sbhsbh".length();
        System.out.println(a);
        //拼接字符串
        String b="bnjjj";
        String c="bjj";
        String d=b.concat(c);
        System.out.println(d);
        //获取指定索引位置的单个字符
        char s="hello".charAt(1);
        System.out.println(s);
        //查找参数字符串和本来字符串中出现的第一次索引位置
        String k="bhbh";
        String l="b";
        int g=k.indexOf("h");
        System.out.println(g);

        }
    }
    
    
  package ta;
/*
字符串截取方法
public string substring(int index), 参数为索引值  截取从参数位置一直到字符串末尾，返回新字符
public string substring(int begin,int end)截取从begin开始，一直到end结束，中间的字符串，包含begin，不包含end
 */
public class tu {
    public static void main(String[] args) {
        String a="bjbj";
        String b=a.substring(2);
        System.out.println(b);

        String c=a.substring(0,2);
        System.out.println(c);
        }
    }
    
  package ta;
/*
字符串转换方法
public char[] toCharArray(),将当前字符串拆分为字符数组作为返回值
public string repalce(CharSquence old,CharSquence new);
将所有出现的老字符串替换成新的字符串，返回替换之后的结果新字符串。
 */
public class tu {
    public static void main(String[] args) {
        char[]a="hjjj".toCharArray();
        System.out.println(a[0]);

        String b="vhbhb";
        String c=b.replace("b","a");
        System.out.println(c);
    }
    }
   
   
   package ta;
/*
字符串分割方法
public String[] split(String regex) 参数是分割条件 按照参数的规则将字符串切分成若干份 分割之后要用数组接收 
 */
public class tu {
    public static void main(String[] args) {
        String b="vhbhb";
        String[] c=b.split("b");
        for (int i = 0; i < c.length; i++) {
            System.out.println(c[i]);
        }
    }
    }
    
    
    
